<%= link_to("<< Back to List", type_attachment_index_path, :class => 'back-link') %>

<div class="type_attachment edit">
  <h2>Update type_attachment</h2>

  <%= form_for(@type_attachment) do |f| %>

    <table summary="type_attachment form fields">
      <tr>
        <th>Name</th>
        <td><%= f.text_field(:name) %></td>
      </tr>
    </table>

    <div class="form-buttons">
      <%= f.submit("Update type_attachment") %>
    </div>

  <% end %>
</div>